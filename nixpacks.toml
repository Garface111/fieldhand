[phases.setup]
nixPkgs = [
  "python312",
  "pango",
  "harfbuzz",
  "glib",
  "gdk-pixbuf",
  "libffi",
  "cairo",
  "fontconfig",
  "freetype",
  "zlib",
]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn src.main:app --host 0.0.0.0 --port $PORT"
